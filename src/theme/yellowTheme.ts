import { createTheme } from '@mui/material';

export const yellowTheme = createTheme({
  palette: {
    primary: {
      main: '#FFDB15',
    },
    secondary: {
      main: '#E1C340',
    },
    error: {
      main: '#E80000',
    },
  },
});
