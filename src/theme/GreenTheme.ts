import { createTheme } from '@mui/material';

export const GreenTheme = createTheme({
  palette: {
    primary: {
      main: '#59981A',
    },
    secondary: {
      main: '#81B622',
    },
    error: {
      main: '#E80000',
    },
  },
});
