import { createTheme, rgbToHex } from '@mui/material';

export const purpleTheme = createTheme({
  palette: {
    primary: {
      main: '#7954A1',
    },
    secondary: {
      main: '#C55FFC',
    },
    error: {
      main: '#E80000',
    },
  },
});
